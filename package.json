{
  "name": "setsuna",
  "version": "1.0.0",
  "private": true,
  "description": "刹那 / SETSUNA — a neon reflex-training game (single-file HTML).",
  "scripts": {
    "test": "node tests/regression.mjs",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
